package com.example.system.repository;

import com.example.system.domain.newTable;
import org.springframework.data.jpa.repository.JpaRepository;

public interface newRe extends JpaRepository<newTable,Long> {
}
